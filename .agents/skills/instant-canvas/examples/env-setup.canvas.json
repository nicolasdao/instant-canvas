{
	"instantcanvas": 1,
	"title": "Set up environment variables",
	"description": "Credentials are written straight to .env — they never enter the chat.",
	"blocks": [
		{
			"type": "form",
			"title": "API credentials",
			"description": "Fill these in once; the agent only learns which keys were set.",
			"destination": { "kind": "env", "path": ".env", "mode": "merge" },
			"submitLabel": "Save credentials",
			"fields": [
				{ "name": "OPENAI_API_KEY", "label": "OpenAI API Key", "type": "secret", "required": true, "help": "Used for embeddings and chat completion." },
				{ "name": "SUPABASE_SERVICE_ROLE_KEY", "label": "Supabase Service Role Key", "type": "secret", "required": true },
				{ "name": "SUPABASE_URL", "label": "Supabase URL", "type": "text", "required": true, "placeholder": "https://xxxx.supabase.co" },
				{ "name": "ENVIRONMENT", "label": "Environment", "type": "select", "options": ["development", "staging", "production"], "default": "staging" }
			]
		}
	]
}
